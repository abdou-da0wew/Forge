package config

// Config is the main configuration structure (alias to the main config)
type Config = config.Config

// This file exists to provide the config types for other packages
// The actual implementation is in internal/config/config.go
